# gstack 教程 No. 2
## 在 WorkBuddy 上构建 AI 智能体驱动的端到端 EDA 系统

**面向研究生与青年工程师的逐步实操教程**

版本 1.0 · 2026 年 9 月 · 文字 CC BY 4.0 · 代码 Apache-2.0

> **一句话概述。** 你将用 5 个 gstack 编排角色 + 2 个领域 AI 智能体，构建、运行并
> **验证**一条真实的开源芯片流程 —— 基于 SkyWater SKY130 开放 PDK 的 RTL → GDS，
> 并用验证门链与字节级哈希证明它真的成立。

---

## 如何使用本教程

| 你是…… | 从哪开始 | 时间预算 |
|---|---|---|
| 研究生，首次接触 | 第 0 部分，然后顺序做完 | 2 天（10–14 小时） |
| 青年工程师，有些 EDA 基础 | 第 0 部分（略读）→ 第 2 部分 → 第 3 部分起 | 1 天 |
| 开课教师 | 第 0–1 部分作讲授，第 2–7 部分作实验，第 11 部分作考核 | 4 × 3 学时 |
| 只评估是否值得采用 | 只读第 0 部分 + 第 8 部分 | 45 分钟 |

**学习约定**

1. 不要跳过第 0 部分。可迁移的是心智模型，不是命令。
2. 每个部分结尾都有**检查点**，不通过不往下走。
3. 命令要自己敲。读 EDA 教程学不会 EDA。
4. 门失败，说明教程正在起作用。慌之前先读第 10 部分。

---

## 前置条件

| 项 | 要求 | 说明 |
|---|---|---|
| 操作系统 | Windows 11（macOS/Linux 走第 2 部分选项 C 的虚拟机） | 已验证链路 |
| 磁盘 | 建议 30 GB 可用，绝对最低 22 GB | PDK 本身 7.3 GB |
| 内存 | 最低 8 GB，16 GB 舒适 | Magic + 布局布线 |
| 宿主 | 腾讯 WorkBuddy（或 Claude Code / OpenAI Codex） | 运行智能体 |
| EDA | Magic、Netgen、SKY130A PDK、Microlane —— 第 2 部分安装 | 工具链 |
| 基础 | Verilog + 基本 CMOS。不需要 AI 背景。 | — |

你**不需要**：商业 EDA 授权、NDA、流片预算，或任何 AI 智能体经验。

---

# 第 0 部分 —— 心智模型

**时间：30 分钟 · 无需敲命令**

## 0.1 gstack 给你什么

gstack 是一套**智能体技能库**，把一个 AI 助手变成一支小型工程组织。每个技能是一条
斜杠命令，有明确的职责、明确的产出、明确的停止点：

| 命令 | 角色 | 职责 | 停止条件 |
|---|---|---|---|
| `/plan-ceo-review` | AI CEO | 挑战范围、定位、野心 | 范围站得住 |
| `/spec` | AI PM | 拆解成可派发的任务项 | 待办清单具体可执行 |
| `/plan-eng-review` | AI 工程师 | 锁定架构、数据流、执行顺序 | 计划可施工 |
| `/qa-only` | AI QC | 测试并报告 —— **不改** | 报告已归档 |
| `/ship` | AI DevOps | 定版、打包、写文档、发布 | 发布已打标签 |

价值不在于 AI 有多聪明，而在于**每个角色都有停止点** —— 于是决策被审计，而不是被静默吞掉。

## 0.2 教程 No.1 教了什么，本教程补什么

[教程 No.1](https://github.com/mikelix/gstack-tutorial-1) 把这个闭环跑在一行
`hello_world.py` 上。那是正确的选择：没有真实产物，就没有东西干扰你对流程的理解。

但它留下三件事没教，而这恰恰是在职工程师天天撞上的：

| | 教程 #1 | 教程 #2 |
|---|---|---|
| 载体 | `hello_world.py` | 真实的 RTL→GDS EDA 系统 |
| 团队 | 5 个 gstack **评审**角色 | 5 个 gstack 角色 **× 2 个领域智能体** |
| 完成判据 | 评审意见合并 | **门链全 PASS + GDS SHA 一致** |
| 环境 | 无 | 可复现的开源 EDA + 开放 PDK |
| 价值叙事 | — | 一套独立估值约 125 万元人民币的系统 |

> **一句话的差别：**
> #1 里智能体**评审**工作；#2 里智能体**做**工作，并被验证门卡住。

## 0.3 三层模型

```
  第 1 层 —— gstack 编排        /plan-ceo-review → /spec
  （决策与审计轨迹）              → /plan-eng-review → /qa-only → /ship
                │
                │  派发任务
                ▼
  第 2 层 —— 两个领域智能体
                • AI GDS-Architect            数字：RTL→GDS、DRC、LVS、确定性
                • 资深模拟 IC 设计架构师        模拟：sizing、仿真、版图意图
                │
                │  每件产物都被判定
                ▼
  第 3 层 —— 开源 EDA + 开放 PDK + 门链
                Magic 8.3.681 · Netgen 1.5.323 · KLayout · ngspice · Xschem
                SKY130A（open_pdks 1.0.572）· Microlane 布局布线
                门 5 DRC → 门 6 提取 → 门 7A LVS → 门 7B-1R2 LVS
```

中间层没有一个是摆设。每个智能体都有**交接契约**：收到什么、必须产出什么、由哪道门判定。
一个产出的文件没有任何门去检验的智能体，是在写作业，不是在做工程。

## 0.4 能力缺口 —— 以及谁有权决定什么

这是第 0 部分最重要的一节，也正是教程 No.1 不需要写的一节。

5 个 gstack 角色擅长的是**流程**：排序、拆解、审计、打包。它们天生是通才。
它们**没有半导体工程专业知识，也不掌握半导体设计流程的实际运作方式**。
这不是缺陷 —— 正因如此它们才是好的编排者。但这意味着：**领域问题上，它们绝不能是最终权威。**

| gstack 角色会很自然地…… | ……但它不知道 |
|---|---|
| 接受"LVS 通过" | 朴素的 LVS 可能是循环论证，什么也证明不了 |
| 接受"DRC 干净" | 实际跑了哪几类规则、又豁免了什么 |
| 为了整洁重排构建顺序 | 流式写出顺序会改变 GDS 字节流，直接破坏确定性 |
| 建议"放宽一下检查就能变绿" | 放宽签核检查是唯一不可饶恕的动作 |
| 把现有产物直接打包 | 没有可复现哈希的 GDS 不叫发布版 |
| 起草定位文案 | 一个写错的物理数字会毁掉全部可信度 |

两个领域智能体补上的，正是编排层缺的那一半：25 年以上 CMOS/MEMS 与半导体工程判断力，
以及"RTL 如何变成硅片"的流程知识。所以这个团队不是"5 + 2"，而是
**5 个决定流程，2 个决定物理 —— 物理压过流程。**

### 权限契约

| 决策事项 | gstack 角色 | 领域智能体 | 人类 |
|---|---|---|---|
| 范围、阶段划分、任务拆解 | **决定** | 被咨询 | 批准 |
| 选哪个 PDK / 标准单元库 | 无权 | **决定** | 批准 |
| 工具版本锁定 | 记录 | **决定** | — |
| 什么算"门通过" | 执行 | **决定** | — |
| DRC 违例能否豁免 | 无权 | **决定** | 签字 |
| LVS 失配如何解读 | 报告 | **决定** | — |
| 确定性契约 | 记录 | **决定** | — |
| 发布内容与版本号 | **决定** | 被咨询 | 批准 |
| 路线图承诺、对外文案 | 起草 | **必须审核** | 批准 |
| 任何含物理数字的表述 | 无权 | **决定** | — |

### 三条否决规则

1. **领域否决优先于流程便利。** 领域智能体说某步不安全，那一步就不做，计划怎么写都不算。
2. **门的结果不是事实，直到领域智能体读过日志 —— 而不只是状态行。** "PASS"只是一个字符串，日志才是证据。
3. **绝不为让构建通过而放宽检查。** 门失败时，要么产物错了，要么门错了。
   领域智能体必须书面说明是哪一种。任何人不许静默放宽容差。

### 让渡条款（写进每一个 gstack 提示词）

```text
需征询领域智能体。在你敲定任何涉及 DRC、LVS、提取、确定性或 PDK 规则的表述之前，
先将该具体论断交给领域智能体（数字方向交给 AI GDS-Architect，模拟方向交给
资深模拟 IC 设计架构师），并逐字引用其答复。

若领域智能体与你的结论冲突，以领域智能体为准。不要自行裁决 —— 
把两方陈述并列提交给人类裁决。
```

把这一段复制进第 3–7 部分里每一个 `/plan-ceo-review`、`/spec`、
`/plan-eng-review`、`/qa-only`、`/ship` 提示词。成本只有一个段落，
而它是"听起来对"与"确实对"之间的分界线。

> **领域智能体凭什么可信。** AI GDS-Architect 的非循环规则（5.3 节）和确定性
> 契约（6.3 节）不是凭空发明的约定，它们编码的是只有在真实流片多年后才会
> 暴露的失效模式。模拟智能体则带有一条经核验的 22 年证据链 —— 从微混沌混合器
> 的正李雅普诺夫指数（2002），到与何志明教授合著、载于 *J. Fluid Mech.*
> **575**, 425–448（2007）的非线性动力学分析，再到 2023 年获中国一类医疗器械
> 认证的癌症诊断芯片。每一步方法都与实测对照。这正是编排层被要求尊重的标准。

![双钥匙权限模型：gstack 持流程权，两个领域智能体持物理权，二者共同喂给门链，冲突时物理权获胜](docs/two-key-authority.svg)

图 0-1 —— 双钥匙权限模型一图流。带 RACI 列与三条否决权的完整文字版：
[`docs/expertise_division.zh.md`](docs/expertise_division.zh.md)。

## 0.5 两个领域智能体

| 智能体 | 负责 | 产出 | 由谁判定 |
|---|---|---|---|
| **AI GDS-Architect** | 数字后端：综合交接、布局布线、DRC、LVS、流出版图 | GDS、提取网表、门日志 | 门 5、6、7A、7B-1R2 |
| **资深模拟 IC 设计架构师** | 模拟前端：拓扑、sizing、仿真、版图意图 | 已 sizing 的电路图、仿真结果、版图约束 | ngspice 仿真、DRC、（7B-2 在路线图上） |

本教程中数字智能体走主线（它的门已实现）。模拟智能体在第 9 部分接入 —— 而第 9 部分之所以存在，
正因为模拟这一半才是 25 年 CMOS/MEMS 功力的用武之地。

## 0.6 为什么用 SKY130，而不是商业 PDK

作者持有受 NDA 约束的 PDK（TSMC、UMC、GlobalFoundries、ams 0.35 µm、粤芯）。
**它们一个字节都不能出现在本仓库。**

1. NDA PDK 不能公开、不能转发、不能提交进仓库。
2. 夹带闭源 PDK 的"开源教程"不是开源 —— 是带 README 的泄密。
3. SKY130 是首个以开放许可发布的代工级 PDK，**DRC/LVS/RCX 规则同样开放**，
   所以本教程的每一道门，读者都能重跑、审计、fork。
4. 没有损失：方法与工艺无关。门链结构、确定性契约、智能体工作流可平移到任何
   PDK —— 包括那些闭源的。

> **这一节本身就是教学点。** 它用作者真实做过的一个决定（而不是假设情境），
> 教会读者"什么可以开源"。

## 0.7 什么叫"完成"（硬指标，不是愿景）

| # | 判据 | 证据 |
|---|---|---|
| 1 | 门 5 —— Magic DRC | PASS，0 violation |
| 2 | 门 6 —— Magic 提取 | PASS |
| 3 | 门 7A —— 结构级 LVS | PASS |
| 4 | 门 7B-1R2 —— Netgen 层次化 LVS | PASS，出现 `Circuits match uniquely.` |
| 5 | **确定性** | GDS SHA-256 跨次运行可复现 |
| 6 | **双语文档** | 英文 + 简体中文 |

任何一条不满足，就不算完成。没有"基本完成"。

### 检查点 0

- [ ] 你能说出 5 条 gstack 命令及各自的停止点。
- [ ] 你能用一句话讲清教程 #1 与 #2 的差别。
- [ ] 你能解释为什么 NDA PDK 不能出现在本仓库。
- [ ] 你能举出一项 gstack 角色**无权**决定的事项，并说出该由谁决定。
- [ ] 让渡条款已复制到随手可粘贴的地方。

---

# 第 1 部分 —— 安装 WorkBuddy 与两个领域智能体

**时间：45 分钟**

## 1.1 安装宿主

1. 下载并安装腾讯 WorkBuddy。
2. 启动并完成登录。
3. 用一个最简提示验证技能系统在线：
   > `请只回复一个词：ready`

智能体规格与宿主无关。若你用 Claude Code 或 OpenAI Codex，把两份 Markdown 简介
（见 1.3）复制到该宿主的 agent/skill 目录即可。

## 1.2 安装 gstack

gstack 以技能套件形式发布。安装后，确认路由器有响应：

```
@skill:gstack   我想评审一份计划，该用哪个技能？
```

应得到路由答复，点名 `/plan-ceo-review`、`/plan-eng-review` 等。

若 `@skill:gstack` 不可用，直接按名字调用技能即可 —— 教程照样能跑，只是少了自动路由。

## 1.3 安装两个领域智能体

若 `~/.workbuddy/skills/` 不存在则先创建，然后放置：

```
~/.workbuddy/skills/
└── e2e-ic-system/
    └── agents/
        ├── ai_gds_architect.md
        └── analog_ic_architect.md
```

**今天就能用的最小智能体简介**（存为 `ai_gds_architect.md`）：

```markdown
---
name: ai-gds-architect
description: 数字后端智能体。用于开放 PDK 上的 RTL 到 GDS 流程：
  布局布线、DRC、提取、LVS、流出版图与确定性构建。
---

# AI GDS-Architect

## 职责范围
开放 PDK（SKY130A）上的数字后端。负责：布局布线、DRC、提取、LVS、
GDS 流出、确定性。

## 硬性规则
1. 报告 PASS 时必须点名工具、命令与日志产物，缺一不可。
2. 绝不为通过而修改门。门失败就报告失败。
3. 每件产物必须可复现：锁定工具版本、固定随机种子、PYTHONHASHSEED=0、
   冻结 GDS 时间戳。
4. 必须写明你**没有**检查什么。对缺口保持沉默本身就是缺陷。

## 输出格式
- 产物：<每个文件的路径>
- 门：表格 —— 门 | 工具 | 状态 | 产物 | 备注
- 确定性：GDS 的 sha256，以及是否与基准一致
- 未知项：明确列出
```

镜像地写一份 `analog_ic_architect.md`，范围为模拟（拓扑、sizing、ngspice 仿真、
版图约束、失配与工艺角分析）。

重启 WorkBuddy，验证两个智能体可被发现：

> `列出你能看到的智能体。`

### 检查点 1

- [ ] WorkBuddy 能回应最简提示。
- [ ] `@skill:gstack` 能路由（或你能按名字调用技能）。
- [ ] 两份智能体简介已位于 `~/.workbuddy/skills/e2e-ic-system/agents/`。
- [ ] 重启后 WorkBuddy 能列出两个智能体。

---

# 第 2 部分 —— 安装开源 EDA 工具链

**时间：30 分钟（虚拟机）到 4 小时（编译）**

这是第一大流失点。三条路径；**如果你从未装过开源 EDA 工具链，走选项 C。**

| 路径 | 平台 | 时间 | 状态 |
|---|---|---|---|
| **A —— 一键安装器** `install_eda.bat` | Windows 11 | 1–3 小时（需编译） | beta |
| **B —— 手动逐步** | 任意 | 2–4 小时 | 文档完整 |
| **C —— TinyTapeout 虚拟机镜像（推荐）** | Windows / macOS / Linux | **约 30 分钟** | 上游维护 |

## 2.1 锁定版本 —— 不要漂移

| 组件 | 版本 | Commit | 来源 |
|---|---|---|---|
| Magic | **8.3.681** | `4432d7e` | github.com/RTimothyEdwards/magic |
| Netgen | **1.5.323** | `bb8a610` | github.com/RTimothyEdwards/netgen |
| open_pdks / SKY130A | **1.0.572** | `54435919` | github.com/RTimothyEdwards/open_pdks |
| Microlane | — | `87079e7f6` | github.com/htfab/microlane |

版本漂移是 SHA 莫名不一致的头号原因。把实际装上的版本记进 `VERSIONS.lock`。

## 2.2 选项 C —— 虚拟机镜像（推荐）

1. 从 <https://github.com/TinyTapeout/analog-virtualbox-vm-sky130a>（Apache-2.0）
   下载 `tinytapeout_analog_vm.ova`（约 5 GB）。
2. 导入**前**先校验官方公布的 SHA-256。
3. **导入前先改默认虚拟机目录** —— 它默认指向系统盘，而系统盘通常正是快满的那个。
   ```bash
   VBoxManage import tinytapeout_analog_vm.ova --vsys 0 --basefolder D:/VMs
   ```
4. 启动。登录 `ttuser` / `magic`。
5. 导入需约 20 GB 可用空间；虚拟盘按 32 GB 预置。

**版本提示：** 虚拟机锁 Magic `8.3.576`、Netgen `1.5.270`；本教程锁 `8.3.681` /
`1.5.323`。**在虚拟机上会得到不同的 GDS SHA。** 这是预期结果，不是失败 ——
重新定基准即可（见第 6.3 节）。

## 2.3 选项 A / B —— Windows 原生安装

在 **Cygwin64** 内编译。两个会耗掉数小时的坑：

1. 必须使用**隔离启动器**（`env -i`、`MSYS2_ARG_CONV_EXCL='*'`）。否则 MSYS 路径转换
   会破坏 Windows 风格路径，而报错看起来像"文件不存在"，不像"路径被改坏了"。
2. 在 source 进来的脚本里，Netgen 的 LVS 要用 **4 参数 Tcl 形式**：
   ```tcl
   lvs [list $f $cell] [list $f $cell] $SETUP $log
   ```
   6 参数形式只在 `-batch lvs` 命令行下有效。混用会报一个看起来像"setup 文件有问题"的错。

## 2.4 先验证再往下走

```bash
magic   -dnull -noconsole --version
netgen  -batch -nogui
cat $PDK_ROOT/sky130A/.config/nodeinfo.json     # 节点、特征尺寸、open_pdks、magic
ls $PDK_ROOT/sky130A/libs.tech                  # magic netgen klayout ngspice xschem ...
```

安装正确时应看到：

```
node           : sky130A
feature size   : 130 nm
open_pdks      : 1.0.572  (commit 54435919)
magic          : 8.3.681  (commit 4432d7e)
libs.tech      : magic netgen klayout ngspice xschem openlane qflow irsim xcircuit iverilog
标准单元库      : 11 个
sky130_fd_sc_hd: 446 个单元
```

> **值得记住的一处更正：** 开放 PDK **自带 DRC、LVS 与 RCX 规则档**
> （`libs.tech/klayout/{drc,lvs}`、`rules.openrcx.sky130A.*`）。
> PEX 与设备级 LVS 的缺口在**我们自己的流程侧**，不在 PDK。是可修补的缺口，不是关上的门。

### 检查点 2

- [ ] `magic --version` 与 `netgen` 都有响应。
- [ ] `nodeinfo.json` 可读，且你已记录其内容。
- [ ] 你知道自己走的是哪条安装路径，并已写入 `VERSIONS.lock`。

---

# 第 3 部分 —— 阶段 1：AI CEO 定范围

**时间：45 分钟 · 命令：`/plan-ceo-review`**

## 3.1 为什么排第一

跳过 CEO 评审，项目就会带着一份没说出口、从没被审视过的范围往前走。这里花十分钟，
省下一周做错东西的时间。

## 3.2 执行

```
@skill:gstack
/plan-ceo-review
```

提示输入时粘贴这段范围陈述（方括号部分自行改写）：

```
项目：构建一套由 AI 智能体驱动的开源端到端 EDA 系统。

交付物：在 SkyWater SKY130A 上完成 RTL -> GDS，用 4 道门链验证
（DRC、提取、结构级 LVS、层次化 LVS），产出字节级可复现的 GDS。

团队：5 个 gstack 角色 + 2 个领域智能体（AI GDS-Architect、资深模拟 IC 设计架构师）。

定位主张：教学/科研用途，不是商业签核。

请回答：
1. 这个范围对单人 10–14 小时是否过大？
2. 时间不够时，先砍哪一块？
3. "教学平台估值约 125 万元人民币"这个说法站得住，还是过头了？
4. 有什么是我没看到的？
```

## 3.3 一份好的 CEO 评审该给你什么

不是夸奖，是四样东西：

1. **按风险（而非按兴奋度）排序的范围清单。**
2. **明确的砍项清单** —— 时间不够时先丢什么。
3. **对你最弱主张的挑战。** 本例里就是那个估值数字。
4. **至少一个你目前答不上来的问题。** 一个都没有，说明这份评审太浅。

## 3.4 把结论写下来

创建 `reviews/01-ceo-review.md`。本教程附一份完整范例：
[`reviews/01-ceo-review.md`](reviews/01-ceo-review.md) —— 请**先写你自己的，再看范例**。
范例的价值在它的*形状*：阻塞项编号、砍项清单按优先级排序、未决问题标注**已转交**
领域智能体，而不是由无权作答的角色硬答。

```markdown
# 评审 01 —— CEO（/plan-ceo-review）
日期：
命令：
采纳的决定：
否决的决定（含理由）：
范围砍项清单：
未决问题：
```

### 检查点 3

- [ ] `reviews/01-ceo-review.md` 已存在，含决定与砍项清单。
- [ ] 你的主张至少有一条被挑战过。

---

# 第 4 部分 —— 阶段 2：AI PM 拆解工作

**时间：45 分钟 · 命令：`/spec`**

## 4.1 执行

```
@skill:gstack
/spec
```

粘贴：

```
把项目拆成可派发的任务项。

以下约束不得更改：
- 锁定工具：Magic 8.3.681、Netgen 1.5.323、open_pdks 1.0.572、Microlane 87079e7f6
- 四道门必须通过：5（DRC）、6（提取）、7A（结构级 LVS）、7B-1R2（Netgen LVS）
- 产出必须字节级可复现（每次运行记录 SHA-256）
- 两个领域智能体：AI GDS-Architect（数字）、资深模拟 IC 设计架构师（模拟）

每个任务项给出：编号、责任角色、输入、产出、判定它的门、预估工时、被什么阻塞。
```

## 4.2 你应该得到的拆解

| 编号 | 任务项 | 责任方 | 由谁判定 |
|---|---|---|---|
| W1 | 工具链安装 + `VERSIONS.lock` | DevOps | 检查点 2 |
| W2 | 智能体安装 + 交接契约 | DevOps | 检查点 1 |
| W3 | `config.env` + 隔离启动器 | 工程师 | 启动器能跑 |
| W4 | 布局布线至 GDS | **AI GDS-Architect** | 门 5、6 |
| W5 | 非循环参考源导出 | **AI GDS-Architect** | 产物存在且非 GDS 派生 |
| W6 | LVS 对（7A 结构级、7B-1R2 层次化） | **AI GDS-Architect** | 门 7A、7B-1R2 |
| W7 | 确定性证明（重跑比对 SHA） | QC | SHA 一致 |
| W8 | 模拟 sizing + 仿真（选修轨） | **资深模拟 IC 设计架构师** | ngspice 仿真 |
| W9 | 双语文档 + 发布包 | DevOps | 完成定义 §0.6 |

完整拆解（含关键路径，以及三个被**上抛**给领域智能体、而不是由 PM 猜测的问题）见
[`reviews/02-spec.md`](reviews/02-spec.md)。注意那三条上抛 —— 那就是让渡条款在起作用。

## 4.3 配置环境

`config.env`（从模板复制，绝不提交密钥）：

```bash
EDA_ROOT=D:/ipason/EDA
PDK_ROOT=$EDA_ROOT/open-pdks-sky130/share/pdk
PDK=sky130A
STDCELL_LIB=sky130_fd_sc_hd
MICROLANE_SRC=$EDA_ROOT/microlane_dbexport/src
MICROLANE_ORIG=$EDA_ROOT/microlane
RUN_DIR=$EDA_ROOT/microlane-e2e-test/e2e-results/sky130A/run01
PROJECT_NAME=my_first_gds
SOURCE_VERILOG=$RUN_DIR/source.v
CYGWIN_ROOT=D:/ipason/EDA/Cygwin
```

可直接编辑的模板在 [`starter/config.env.example`](starter/config.env.example)。
复制后**逐条**改路径，绝不提交结果 —— 它与机器强绑定。

### 检查点 4

- [ ] 九个任务项已归档，含责任方与判定门。
- [ ] `config.env` 已写好并能成功 source。

---

# 第 5 部分 —— 阶段 3：AI 工程师跑通 RTL → GDS

**时间：3–5 小时 · 命令：`/plan-eng-review` · 智能体：AI GDS-Architect**

## 5.1 先跑工程评审

```
@skill:gstack
/plan-eng-review
```

要求它锁定：数据流、产物命名、每道门的位置，以及 **门失败时运行目录怎么处理**。
最后一个问题新人从不问，却总是后悔。

在这个提示词末尾追加**让渡条款**（0.4 节）。AI 工程师是通才：除非你强制它在
领域问题上让渡，它会按"整洁"来排构建顺序。

## 5.2 派发领域智能体

在 WorkBuddy 中激活 **AI GDS-Architect**，提示：

```
你是 AI GDS-Architect。执行任务项 W4：布局布线至 GDS。

输入：
- config.env：<路径>
- 源 Verilog：<路径>
- PDK：sky130A，标准单元库 sky130_fd_sc_hd

要求：
1. 使用隔离启动器（env -i、MSYS2_ARG_CONV_EXCL='*'）。不要用裸 shell。
2. 在 GDS 流出之前，先跑 DB 源导出钩子，使 LVS 参考源来自**活的布局布线数据库**，
   而不是 GDS。在报告里说明为什么这很重要。
3. 冻结 GDS 时间戳；设置 PYTHONHASHSEED=0；显式固定任何随机种子。
4. 所有日志写入 RUN_DIR，使用稳定文件名。

报告：产物（路径）、门状态表、GDS 的 sha256、以及未知项。
```

## 5.3 非循环规则（这是本教程的智识核心）

朴素 LVS 是**循环的**：把从 GDS 提取的网表，与源自同一个 GDS 的网表比对。
它永远通过，因而什么都证明不了。

修正办法，也正是门 7B 后缀 **1R2** 的来历：

- 在流出之前，从**活的布局布线数据库**导出参考源（`db_source_export.py`，
  钩在 `gds_streamout` 之前），而不是从 GDS 导出。
- 去耦电容的电源/地取自权威 PDK CDL/SPICE，而不是版图。
- 然后才用 Netgen 比对。

```
   布局布线数据库 ──► db_source_export.py ──► 参考源 ──┐
                                                        ├─► Netgen LVS
   布局布线数据库 ──► GDS ──► Magic 提取 ──► 版图网表 ──┘
```

同一起源、两条独立派生。这个比对才有诊断力。

## 5.4 跑门

```bash
# 门 5 —— DRC
magic -dnull -noconsole run_drc.tcl

# 门 6 —— 提取
magic -dnull -noconsole run_extract.tcl

# 门 7A —— 结构级 LVS（Microlane 比较器）
python lvs.py

# 门 7B-1R2 —— 层次化 LVS（Netgen）
netgen -batch source run_lvs.tcl
```

`run_lvs.tcl` 内 Netgen 的 4 参数形式：

```tcl
lvs [list $f $cell] [list $f $cell] $SETUP $log
```

**不要手敲这四条命令。** 它们连同循环性守卫与指纹已串在
[`starter/scripts/run_all.sh`](starter/scripts/run_all.sh) 里：

```bash
cd starter
cp config.env.example config.env     # 先把路径全部改好
./scripts/run_all.sh
```

它同时执行评审 03 定下的退出码契约：

| 退出码 | 含义 |
|---|---|
| `0` | 所有门 PASS |
| `1` | 某个门 FAILED |
| **`2`** | 某个门返回 **VOID** —— 参考源来自 GDS，或输入无法验证 |
| `3` | 环境错误（配置错、工具缺失、复用 `RUN_DIR`） |

VOID 拥有独立退出码，正是关键所在：扫一眼的人、或只断言"非零即失败"的 CI，
都无法再把它误当成"检查通过"。

## 5.5 通过长什么样

| 门 | 预期输出 |
|---|---|
| 5 | DRC clean，0 violation（任何豁免须明确列出） |
| 6 | 提取完成；警告逐一枚举 |
| 7A | 结构匹配 |
| 7B-1R2 | `Circuits match uniquely.` |

### 检查点 5

- [ ] 已知路径下存在 GDS 文件。
- [ ] 门 5、6 PASS，且带日志产物。
- [ ] 参考源来自布局布线数据库，且你能说出为什么这很重要。

---

# 第 6 部分 —— 阶段 4：AI QC 验证，并证明确定性

**时间：2–3 小时 · 命令：`/qa-only` · 注意：`/qa-only` 只报告，不改**

## 6.1 组织纪律

> **跑门的人不能认证自己的门。**

若单人作业，就用时间隔开：跑完门，睡一觉，第二天用干净的 shell 和清醒的眼睛验证。
这不是形式主义 —— 这是抵御确认偏误的唯一防线，而确认偏误正是 AI 辅助工程中的头号失效模式。

**再补上本教程专属的第四条：**

> **AI QC 只有在领域智能体读过日志之后，才可以报告门状态。**
> `/qa-only` 是通才。它会忠实地报告 `Circuits match uniquely.`，却永远不会发现
> 这次比对的一端是来自同一份 GDS 的网表 —— 也就是 5.3 节的循环论证。
> 只有 AI GDS-Architect 能抓到这个。

所以正确的顺序是：**领域智能体读日志 → QC 报状态 → 人类签字。** 绝不能 QC 单独定论。

## 6.2 跑 QA

```
@skill:gstack
/qa-only
```

要求：带产物的门状态表、SHA 比对，以及**明确列出哪些没有被检查**。

最后一项最常被省略，也最重要。见
[`reviews/04-qa-report.md`](reviews/04-qa-report.md) —— 它的"未被检查的内容"
表格有八行，是全篇评审中最有用的部分。

## 6.3 证明确定性

在干净的 shell 里完整跑两遍，然后比对：

```bash
sha256sum $RUN_DIR/*.gds
```

记录：

```markdown
# VERSIONS.lock
magic        8.3.681   (4432d7e)
netgen       1.5.323   (bb8a610)
open_pdks    1.0.572   (54435919)
microlane    —         (87079e7f6)
platform     Windows 11 + Cygwin64
第 1 次 sha256 6ef1c9c9…322bf750
第 2 次 sha256 <必须一致>
```

**两个平台，两个基准。** Windows/Cygwin 链与 TinyTapeout 虚拟机链**必然**产生不同
SHA（PDK 构建不同、工具修订不同）。两个都发布。不要用放松检查去"修好"不一致 —— 去查原因。

## 6.4 报告到频道

发到 `#gate-results`：

```
gate: 7B-1R2
tool: netgen 1.5.323
status: PASS
sha256: <gds sha>
artifact: <路径>
notes: <豁免、警告、任何评审者必须知道的事>
```

### 检查点 6

- [ ] 门 7A、7B-1R2 PASS，且带产物。
- [ ] 两次独立运行 SHA 一致，已记入 `VERSIONS.lock`。
- [ ] `reviews/04-qa-report.md` 列出了**没有**被检查的内容。

---

# 第 7 部分 —— 阶段 5：AI DevOps 发布

**时间：1–2 小时 · 命令：`/ship`**

## 7.1 执行

```
@skill:gstack
/ship
```

范例：[`reviews/05-ship.md`](reviews/05-ship.md) —— 发布清单、双语一致性检查、
许可表、以及四文件规则的审计表。

## 7.2 发布里必须有什么

| 项 | 理由 |
|---|---|
| `VERSIONS.lock` | 可复现性 |
| 四道门的日志 | 证据 |
| GDS + 参考 SHA | 产物及其指纹 |
| `README.md`（英）+ `README.zh.md`（中） | 双语，不可妥协 |
| 已知局限章节 | 诚实是功能 |
| 许可 | 文字 CC BY 4.0，代码 Apache-2.0 |

## 7.3 四文件规则

从教程 #1 原样搬过来，因为它仍然成立：

> **一个概念变更 = 4 个文件 × 4 次校验。**

改一处门定义，就要更新：门脚本、英文文档、中文文档、评审记录 —— 并且各自复核一遍。
为它排预算。不要在最后一天才发现这件事。

## 7.4 把局限写清楚

照抄这个结构，不要软化：

```markdown
## 本版已具备的能力
- [x] 门 5（DRC）、门 6（提取）、门 7A、门 7B-1R2 —— 全部 PASS
- [x] 字节级可复现的 GDS
- [x] 两个带交接契约的领域智能体
- [x] 双语文档

## 路线图（v1.1+）
- [ ] PEX / RCX —— 规则随 PDK 提供，缺的是流程环节
- [ ] 后仿真 —— ngspice 可用，尚未接入
- [ ] 完整 STA —— OpenLane/OpenSTA 可用，但依赖 Docker
- [ ] 门 7B-2 设备级 LVS —— KLayout 规则随 PDK 提供，尚未接入
- [ ] macOS/Linux 已验证执行

## 定位
教学/科研目标：覆盖约 70–85%。
商业签核：约 15–25%。这不是 Cadence 级签核工具。
```

### 检查点 7

- [ ] 发布已打标签；六条完成定义判据均有证据。
- [ ] 双语均已交付；局限章节坦率而非软化。

---

# 第 8 部分 —— 本教程真正教的两条纪律

命令会过时，这两条习惯不会。

## 8.1 纪律一 —— 锁住产物

锁定工具版本 + `PYTHONHASHSEED=0` + 固定随机种子 + 冻结 GDS 时间戳 ⇒
字节级一致的输出 ⇒ 一个你可以公开发布的哈希。

为什么重要：AI 智能体几分钟就能产出"看起来对"的东西。在"看起来对"与"工程"之间，
唯一的屏障是**一个无法被辩倒的裁判**。哈希就是这样的裁判。

## 8.2 纪律二 —— 不只查状态，要查数字

门可以全 PASS 而物理仍然是错的。教程附两个实例，均出自作者本人工作：

| 案例 | 预测 | 实测 | 误差 |
|---|---|---|---|
| 四轴悬停功率（GDA） | 127.8 W | 127.4 W | 0.29% |
| CTC 捕获临界毛细数 Ca\* | 0.043 | 0.04 ± 0.006 | 落在实验带内 |

两者都经得起独立的第一性原理交叉核验。悬停一例：m = 1.04 kg ⇒ T = 10.20 N；
9.4 英寸桨 ⇒ A = 0.179 m²；致动盘理论给出 P_ideal = 49.2 W；实测 127.4 W ⇒
反推效率因子 **0.386**，正确落在低雷诺数 9.4 英寸桨的 0.35–0.45 带内。

**并且两者都主动写明自己可被证伪的前提：**

> FM = 0.39，独立取自桨叶数据 —— **未**用本次飞行标定。

这一句话就是整个教训。主动交代"在哪个前提下我的数字会错"，是把主张变成证据的动作。
内行听众会因此更信你，而不是更不信。

> **练习：** 为你的 GDS 写出等价的那一句话。（提示：与 PDK 构建 commit 有关。）

---

# 第 9 部分 —— 接入模拟智能体

**时间：2–4 小时 · 智能体：资深模拟 IC 设计架构师**

上面的数字链路已全程设门。模拟这一半，才是第二个智能体真正值钱的地方 ——
也是 CMOS/MEMS 背景最见功力的地方。

## 9.1 模拟智能体负责什么

| 阶段 | 工具 | 产物 |
|---|---|---|
| 电路图输入 | Xschem | `.sch` |
| Sizing / 工作点 | 手算 + ngspice op | sizing 表 |
| 仿真（DC、AC、tran、噪声） | ngspice | `.raw`、实测指标 |
| 工艺角与失配 | ngspice 扫描 | 工艺角表 |
| 版图意图 | Magic | 约束说明 → 版图 |

## 9.2 派发提示词

```
你是资深模拟 IC 设计架构师。在 SKY130A 上设计并仿真一个 <拓扑>。

指标：<增益、带宽、噪声、功耗、电源、负载>
工艺角：tt/ss/ff、<温度>、VDD ±10%。
交付：已 sizing 的电路图、ngspice 网表、仿真指标 vs 指标要求对照表，
      以及最可能出错的那一个假设。
明确说明你**没有**仿真的内容。
```

## 9.3 为什么这对你尤其重要

作者本人的研究线索是：非线性动力学 → 微混沌混合（λ > 0、KAM 环面破裂、FTLE 脊线）
→ Strouhal 标度律 → 混沌混合器进入 CTC 捕获芯片（*Angew. Chem. Int. Ed.*
50(13):3084–3088，封面）→ 非线性二自由度 m–c–k 细胞模型 → 临床验证与认证。

从一个李雅普诺夫指数到一台获证诊断设备，走了 22 年。领域在变，纪律没变。
**而"锁住产物、核查数字"这条纪律，正是本教程用门与哈希去自动化的东西。**

## 9.4 模拟轨的诚实状态

本版本中模拟智能体的门**尚未实现**。它的产出由仿真与人工评审判定，不由门链判定。
在你的报告里要写明这一点。

### 检查点 9

- [ ] 存在已 sizing 的电路图与 ngspice 网表。
- [ ] 仿真指标 vs 指标要求对照表已填。
- [ ] 智能体点名了一个最可能出错的假设。
- [ ] 对每一次派发，你都能说出用到了哪个 AI 层级（见 9.7）**以及是哪个引擎
      验证了结果** —— 不是哪个聊天机器人，是哪个引擎。

---

## 9.5 模拟智能体执行的工作流

第 5 部分教的是数字主线：RTL → GDS 走四道门。让模拟智能体设计一个低噪声 CMOS
传感器放大器，它**不会**先画电路图。它从传感器和产品指标开始，按顺序走完
大致二十个阶段：

| # | 阶段 | 产出喂给谁 |
|---|---|---|
| 1 | 传感器建模与产品指标定义 | 之后所有数字都要对着它判定的目标 |
| 2 | 信号 / 噪声 / 动态范围预算 | 逐模块分配 —— 指标变成算术 |
| 3 | PDK 器件特性表征 | 真实的 f_T、噪声、失配数据，不是教科书模型 |
| 4 | 架构对比 | 候选拓扑在纸面上被淘汰，而不是在硅片上 |
| 5 | 基于 g_m/I_D 的初步 sizing | 用查表法得初始 W、L、I_D，不靠猜 |
| 6 | 手工计算 | 仿真结果必须与之对照的那个数 |
| 7 | 标称 SPICE | 手算数字与物理的第一次对峙 |
| 8 | 噪声贡献分析 | 哪个器件吃掉了噪声预算 |
| 9 | 架构 / 系统行为级仿真 | 进晶体管级苦战之前先看环级行为 |
| 10 | PVT 角 | 能否挺过工艺、电压、温度 |
| 11 | 蒙特卡洛 / 失配 | 要的是分布，不是一个点 |
| 12 | 自动优化 / 设计中心化 | 移动的是分布，不是标称值 |
| 13 | 早期 floorplan | 关键器件在布线存在之前就位 |
| 14 | 关键器件版图 | 匹配与寄生靠构造保证 |
| 15 | 渐进式 PEX | 寄生逐步提取，不是最后一步才做 |
| 16 | 后版图噪声 / 稳定性 / PVT | 你真正会拿到的数字 |
| 17 | 高良率 / high-sigma 分析 | 六西格玛角，不是三西格玛的侥幸 |
| 18 | 可靠性 / 老化 / EMIR | 十年后它还得能工作 |
| 19 | AMS / 全芯片验证 | 模拟模块放进它的数字环境里 |
| 20 | 流片 → 表征 → 模型相关性 | 实测硅片 vs 预测硅片 |

第 1–3、10–12、16–17、20 阶段正是本教程门哲学的用武之地：每一阶段产出一个
产物，它要么与之前的产物一致，要么带着证据解释为什么不一致。

**给研究生的话：** 先学这套顺序，再学任何工具。顺序本身就是 expertise，
工具是可以换的。

## 9.6 方法论，一行说完

整套工作流压缩成一行：

```
Specs → Physics → Architecture → Optimization → Statistics → Layout → Silicon
```

而**不是**：

```
画电路图 → SPICE → 画版图
```

这条链上的每个箭头都是第 5 部分意义上的一扇门：有东西穿过它（一个产物、
一个数字、一个分布），也有东西判定它（一个手算值、一次 corner 扫描、一次实测）。
如果你说不出是什么产物穿过了某个箭头，那这个箭头就没有发生 —— 把该阶段如实
记为 VOID，继续往前走。这和你在 LVS 上用的退出码纪律是同一件事：
没有产出可对照产物的阶段没有通过，它只是跑过。

## 9.7 AI 该待在哪 —— 四个层级

把模拟 IC 设计中的 AI 用法分成四层。层的划分标准不是 AI **做什么**，
而是**由什么来验证它**：

| 层级 | AI 做什么 | 由谁验证 | 2026 年现状 |
|---|---|---|---|
| **1 —— 工程助手** | 推导公式；写 Ocean/SKILL/Python 脚本；生成测试台；分析仿真日志；画 trade-off 图 | 工程师逐条读它产出的每一样东西 | 今天就非常好用 |
| **2 —— 设计空间优化器** | 在你设的约束内选 W、L、I_D、C、R | 仿真器 —— 唯一的裁判 | 约束设得对就非常好用 |
| **3 —— 版图助手** | 布局、布线、约束捕获、寄生优化 | DRC/LVS 门 + 版图感知检查 | 进展很快：Synopsys 公开描述了一条 AI 辅助模拟版图综合流程，能捕获设计意图与约束、产出 DRC 干净的版图并做版图感知优化；2026 年报告了客户部署进展与版图迭代次数的大幅下降 |
| **4 —— 自主工程智能体** | 端到端编排 EDA 任务 | 基于物理的 EDA 引擎，持续不断地验证 | 正在涌现：厂商开始集成能编排 EDA 任务的 agentic 系统，但其结论要持续对照基于物理的 EDA 引擎 —— Siemens 明确描述的是这种"自验证"智能体模型，而不是让 LLM 单独裁定正确性 |

这个区分至关重要，而且它就是本教程的全部要点：

> **第 4 层就是本教程用开源工具构建的东西 —— 只是换了个名字。**
> 双钥匙权限契约（图 0-1）是同一个思想：**LLM 永远不裁定正确性，物理引擎裁定。**
> 如果有厂商承诺自主模拟设计却不点名验证它的引擎，你现在知道该问哪个问题了。

---

# 第 10 部分 —— 故障排查

| 症状 | 可能原因 | 处置 |
|---|---|---|
| `magic: command not found` | 不在 PATH，或 Cygwin 未用隔离方式启动 | 用启动器；检查 `config.env` |
| GDS SHA 两次运行不一致 | 版本漂移，或时间戳未冻结 | 复核 `VERSIONS.lock`；冻结时间戳；`PYTHONHASHSEED=0` |
| 虚拟机上 SHA 不同 | PDK 构建（`bdc9412`）与工具修订不同 | 预期结果 —— 发布第二个基准 |
| Netgen 报 setup 文件错 | 在 source 脚本里用了 6 参数形式 | 改用 4 参数：`lvs [list $f $cell] [list $f $cell] $SETUP $log` |
| 路径被破坏（`D:ipason` 少了斜杠） | MSYS 路径转换 | `env -i` + `MSYS2_ARG_CONV_EXCL='*'` |
| LVS 通过得太轻松 | 循环比对（GDS 比 GDS） | 确认参考源来自布局布线数据库 |
| 门通过但结果是错的 | 门只验形式，不验物理 | 纪律二：按第一性原理重算 |
| Windows 安装失败 | — | 依次回退：选项 C（虚拟机）→ 选项 B（手动）→ 提 issue |

提 issue 时务必附上：

```bash
magic --version; netgen --version; cat $PDK_ROOT/sky130A/.config/nodeinfo.json
```

---

# 第 11 部分 —— 考核（教学用）

## A. 实验题

1. **复现。** 跑两遍链，发布两个 SHA。（考察确定性。）
2. **故意破坏。** 把标准单元库换成 `sky130_fd_sc_hs` 再跑，解释 SHA 的变化。（考察版本纪律。）
3. **循环 LVS。** 用 GDS 派生网表比 GDS 派生网表。证明它会通过。解释它为什么毫无价值。（考察非循环规则。）
4. **物理 vs 状态。** 找一道通过的门，指出它**没有**检查的量，手工算出来。（考察纪律二。）
5. **智能体问责。** 让 GDS 智能体给出一个没有产物的 PASS。说明简介里的硬性规则本该如何拦住它。（考察交接契约。）

## B. 笔试

1. 为什么 NDA PDK 不能出现在开源教程里？给两条理由。
2. 用一句话说明教程 #1 与 #2 的差别。
3. 门 7B-1R2 凭什么是非循环的？
4. 同事说"门全过了，所以设计是对的"。请回应。
5. 更新工具后你的 GDS SHA 变了。这是 bug 吗？你该怎么做？

## C. 评分标准

| 判据 | 权重 |
|---|---|
| 四道门均 PASS 且带产物 | 30% |
| SHA 可复现且已记录 | 20% |
| 局限陈述诚实且具体 | 20% |
| 演示了纪律二（某数字手工重算） | 20% |
| 双语输出或文档清晰 | 10% |

---

# 第 12 部分 —— 接下来

| 方向 | 增加什么 | 工作量 |
|---|---|---|
| **PEX / RCX** | 把 `rules.openrcx.sky130A.*` 接进链 | 中 —— 规则随 PDK 提供 |
| **门 7B-2** | 用 KLayout + `libs.tech/klayout/lvs` 做设备级 LVS | 中 —— 规则随 PDK 提供 |
| **后仿真** | 在提取网表上跑 ngspice | 中 |
| **完整 STA** | OpenLane / OpenSTA | 高 —— 依赖 Docker |
| **流片** | TinyTapeout  shuttle，100 美元起 | 低（GDS 干净之后） |
| **模拟门链** | 给模拟智能体装上真正的门 | 高，但价值高 |

**站在这些肩膀上：**

| 项目 | 角色 |
|---|---|
| [google/skywater-pdk](https://github.com/google/skywater-pdk) | 开放许可的代工级 PDK |
| [open_pdks](https://github.com/RTimothyEdwards/open_pdks) | PDK 构建与安装框架 |
| [Magic](https://github.com/RTimothyEdwards/magic) · [Netgen](https://github.com/RTimothyEdwards/netgen) | DRC / 提取 / LVS |
| [Microlane](https://github.com/htfab/microlane) | 轻量开源布局布线 |
| [Tiny Tapeout](https://tinytapeout.com)（Matt Venn、Uri Shaked） | 低成本流片通道 |
| [Zero to ASIC Course](https://zerotoasiccourse.com) | 教同一套工具，但**不用** AI 智能体 |
| [SiliWiz](https://app.siliwiz.com) | 浏览器里的硅直觉，Apache-2.0 |

**定位，直说：** Zero to ASIC 教人用开源工具做芯片；本教程教用 **gstack + 两个领域智能体**
跑同一条流程，并用门链与哈希锁死产出。相邻层，不是竞争关系。

---

# 附录 A —— 提示词库

**CEO**
```
/plan-ceo-review
挑战这份范围。先砍哪一块？我哪条主张最弱？有什么是我没看到的？
```

**PM**
```
/spec
拆成任务项，每项含：责任方、输入、产出、判定门、工时、阻塞项。
以下约束不得更改：<列出>
```

**工程师**
```
/plan-eng-review
锁定数据流与产物命名。门失败时运行目录怎么处理？每道门放在什么位置？
```

**领域智能体派发（务必包含这四行）**
```
1. 说明你产出的每件产物及其路径。
2. 说明每件事由哪道门判定，状态如何。
3. 说明 GDS 的 sha256，以及是否与基准一致。
4. 说明你**没有**检查什么。
```

**QC**
```
/qa-only
只报告，不改。包含：带产物的门状态表、SHA 比对，以及明确列出未被检查的内容。
```

**DevOps**
```
/ship
定版、打包、写双语文档、不软化地陈述局限、打发布标签。
```

---

# 附录 B —— 评审记录模板

```markdown
# 评审 NN —— <角色>（</命令>）
日期：
命令：
评审输入：

## 发现
| # | 发现 | 严重度 | 处置 |
|---|---|---|---|

## 采纳的决定
## 否决的决定（含理由）
## 未决问题
## 未被评审的内容
```

---

# 附录 C —— 版本锁定卡（打印这一页）

```
Magic          8.3.681   4432d7e
Netgen         1.5.323   bb8a610
open_pdks      1.0.572   54435919
Microlane      —         87079e7f6
PDK            sky130A   130 nm，5 层金属 + MiM + RDL
标准单元库      sky130_fd_sc_hd（446 个单元）
PYTHONHASHSEED 0
GDS 时间戳      冻结（1970）
基准 SHA        Windows/Cygwin：6ef1c9c9…322bf750
                TinyTapeout 虚拟机：<你自己的基准>
```

---

# 附录 D —— 术语表

| 术语 | 含义 |
|---|---|
| **DRC** | 设计规则检查 —— 几何是否违反代工规则 |
| **LVS** | 版图对电路图 —— 两个网表是否描述同一电路 |
| **PEX / RCX** | 寄生提取 —— 把几何转成 R 与 C |
| **STA** | 静态时序分析 |
| **GDS** | 交付给代工厂的最终版图数据库 |
| **PDK** | 工艺设计包 —— 某一工艺的规则、模型、单元库 |
| **门** | 一个含工具、命令、判据与产物的自动检查 |
| **确定性契约** | 使产出字节级可复现的那组锁定项 |
| **交接契约** | 智能体收到什么、产出什么、由谁判定 |
| **非循环 LVS** | 比对了自活数据库的两条派生，而不是 GDS 自比 |

---

## 许可

文字采用 **CC BY 4.0**，代码采用 **Apache-2.0**。本教程引用的商业产品
`e2e-ic-system` 不包含在内，仍受其独立许可约束。

**完成状态：四道门全通过、SHA 可复现，才算学完。**
